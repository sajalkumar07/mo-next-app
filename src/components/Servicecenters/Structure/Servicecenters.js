import React from 'react';

const Servicecenters = ({ brand }) => { 
    // console.log(brand) // Destructure brand from props
  return (
    <div>{brand}</div>  
  );
}

export default Servicecenters;